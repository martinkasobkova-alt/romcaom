{
  "name": "romcaom",
  "private": true,
  "description": "Monorepo: frontend/ (statický web) + backend/ (Express API)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm run start -w beyond-limits-backend",
    "dev": "npm run dev -w beyond-limits-backend",
    "build": "npm run build -w beyond-limits-backend",
    "hash-password": "npm run hash-password -w beyond-limits-backend"
  }
}
